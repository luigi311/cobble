[package]
name = "cobble-config"
version = "0.1.0"
edition = "2024"
description = "Shared Cobble daemon and GUI configuration model."

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
toml = "0.8"
