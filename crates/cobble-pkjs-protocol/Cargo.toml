[package]
name = "cobble-pkjs-protocol"
version = "0.7.0"
edition = "2024"
description = "Private IPC protocol between cobbled and its PebbleKit JS helper."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
